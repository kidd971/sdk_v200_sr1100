{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "CMake: Build Puretone Master RJF (incremental)",
            "type": "shell",
            "command": "${workspaceFolder}/.pixi/bin/pixi.exe run build-puretone-master-rjf",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "CMake: Clean Rebuild Puretone Master RJF",
            "type": "shell",
            "command": "${workspaceFolder}/.pixi/bin/pixi.exe run clean-build-puretone-master-rjf",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [],
            "group": "build"
        },
        {
            "label": "CMake: Clean Rebuild All Puretone Variants (12 bins)",
            "type": "shell",
            "command": "${workspaceFolder}/.pixi/bin/pixi.exe run clean-build-puretone-variants",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [],
            "group": "build"
        },
        {
            "label": "[DFU/STM32] Flash binary",
            "type": "shell",
            "command": "${workspaceFolder}/.pixi/bin/pixi.exe run flash ${command:cmake.selectLaunchTarget}",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": []
        }
    ]
}
