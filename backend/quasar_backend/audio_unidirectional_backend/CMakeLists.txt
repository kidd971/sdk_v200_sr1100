target_link_libraries(audio_unidirectional_backend PRIVATE hardware module_tinyusb_baremetal)
target_sources(audio_unidirectional_backend PRIVATE audio_unidirectional_backend.c)

if (USB_AUDIO_ENABLED)
    target_link_libraries(audio_unidirectional_backend PRIVATE middleware_tinyusb module_tinyusb_audio)
    target_sources(audio_unidirectional_backend PRIVATE audio_unidirectional_tinyusb_backend.c)
else()
    target_link_libraries(audio_unidirectional_backend PRIVATE max98091_driver)
    target_sources(audio_unidirectional_backend PRIVATE audio_unidirectional_i2s_backend.c)
endif()
