# 為什麼是 Mono？—— 近端遮擋斷音的根因與對策

**結論先講：** 近距離遮擋造成的斷音，不是訊號強度不夠，是**每個封包能被重送的次數不夠**。
在這個架構下，重傳餘裕只能用「壓縮」買，不能用「加大封包」買 —— 而 48 kHz ADPCM **Mono**
是唯一能同時做到「每包涵蓋更多音訊」和「payload 更小」的組合。這也是 SPARK 自己在
v1.4.0 給出的答案。

日期：2026-08-04 ｜ 平台：Quasar U5A5 + SR1100，single radio ｜ SDK v2.3.0 + v2.3.1 delta

---

## 1. 問題

近距離、有人體或物件遮擋時，音訊會斷。這個問題在 REV C3 / v1.4 beta5 那個世代測試時
曾經被解掉，但在目前的 puretone 韌體上又出現，且**頻率與位置與韌體改版無關**（見 §2）。

---

## 2. 排除了什麼（都有實測）

| 嘗試 | 結果 | 說明 |
|---|---|---|
| **FEC 1.25 → 2.0** | ❌ 開機紅燈 | 前導碼/編碼變長吃不下 250 µs timeslot，init 直接 assert |
| **ISI mitigation 0 → 2 / 3** | ❌ 無改善 | 連得上、有聲音，斷音頻率位置不變 |
| **ISI 0 → 1**（SPARK 出貨值） | ❌ 無改善 | — |
| **最後一階改 24 kHz**（payload 54 B → 31 B） | ❌ 無改善 | 出得了聲，但斷音不變 |
| **未改版的 baseline** | 同樣會斷 | **對照組：頻率、位置完全一樣** |

**共同點：以上每一項改的都是「單次發送有多耐撞」，沒有一項增加「可以撞幾次」。**
這正是它們全部無效的原因。

---

## 3. 根因：重傳餘裕

排程是 250 µs × 9 槽 = 2.25 ms 一圈，Coordinator 佔 7 槽 → 每秒 3111 次發送機會。
每包能被送幾次，取決於**累積比例（accumulator ratio）** —— 它決定一個封包裝多少音訊：

| Fallback 階 | acc | 每包涵蓋 | 每秒需要 | **每包可用發送次數** |
|---|---|---|---|---|
| 0　96k 24-bit | 1.0× | 0.42 ms | 2400 | **1.3** |
| 1　48k 24-bit | 1.7× | 0.71 ms | 1412 | **2.2** |
| 2　48k 16-bit | 1.7× | 0.71 ms | 1412 | **2.2** |
| 3　48k ADPCM 立體 | 2.3× | 0.96 ms | 1043 | **3.0** |
| **4　48k ADPCM Mono** | **4.2×** | **1.75 ms** | **571** | **5.4** |

而且這些槽還要跟 data 連線共用，實際更少。

**遮擋是突發性衰減。** 降階機制是**反應式**的（要等 buffer 堆積或 CCA 惡化才動作），
等它反應完，音訊已經斷了。能扛住突發的只有**事前就存在的重傳餘裕**。

### 實測佐證

把 mode 2 的降階門檻放寬（讓它更早退到 mode 3）後：

- fb 確實更常待在 3 ✅ —— 機制如預期運作
- **但斷音跟著搬到 fb=3** ❌ —— 階梯撞到底了，而底部的 3.0 次仍然不夠

這一步把假設驗證完了：問題不在「哪一階」，在**整條階梯的底部餘裕不足**。

---

## 4. 為什麼不能直接把封包加大？

最直覺的做法是「把 mode 2 的累積比例拉高」，但**做不到**：

mode 2 是 **48 kHz 16-bit 未壓縮**，累積比例拉高 → payload 等比例變大：

| mode 2 accumulator | payload | 發送次數 |
|---|---|---|
| 17/10（現行） | 138 B | 2.2 |
| 30/10 | **242 B** | 3.9 |

而 SWC 的 fallback threshold **必須遞減**，`swc_connection_set_fallback_cfg()` 是硬檢查：

```c
CHECK_ERROR(cfg->thresholds[i] <= cfg->thresholds[i + 1], err, SWC_ERR_INVALID_PARAMETER, return);
```

現行是 `{206, 138, 54}`，mode 2 變 242 就成了 `{206, 242, 54}` —— 遞減破了，
**開機直接紅燈**。而且 242 剛好等於未降級時的完整 payload，那一階等於沒有降級效果。

> **關鍵限制：在這個架構下，重傳餘裕是用壓縮買的，不是用加大封包買的。**

---

## 5. 為什麼是 Mono

Mono 是唯一同時滿足「累積更多」與「payload 更小」的組合 —— 聲道砍半抵掉了累積變大的成本：

| | acc | payload | 發送次數 | 遞減成立？ |
|---|---|---|---|---|
| mode 3（ADPCM 立體） | 2.3× | 54 B | 3.0 | — |
| **mode 4（ADPCM Mono）** | **4.2×** | **47 B** | **5.4** | ✅ 47 < 54 |

**發送機會從 3.0 提升到 5.4，接近翻倍。**

### 與其他候選方案的比較

| 方案 | 發送次數 | 需要新的 SRC | 切換接縫 | 音質代價 |
|---|---|---|---|---|
| 24 kHz 立體（已實作） | 3.0（**沒有增加**） | ❌ 需要 ÷4 / ×4 | ❌ 有爆音 | 頻寬掉到 ~7 kHz（悶） |
| 24 kHz + 提高累積 | 5.4 | ❌ 需要 | ❌ 有 | 同上 |
| **48 kHz ADPCM Mono** | **5.4** | ✅ **沿用現有 ÷2** | ✅ **無** | 失去立體聲像，**頻寬維持 48 kHz** |

24 kHz 那條路必須換重取樣器，而換 SRC 會在 mode 2↔3 之間造成波形接縫（實測聽得到爆音）。
要消除它得補 discard 保溫機制，那會讓同一條鏈上出現兩個 discard-capable 的 SRC ——
**SDK 從未跑過這個組合**，是本專案評估過風險最高的一段。

**Mono 完全避開這一切**：重取樣器從頭到尾都是同一顆 ÷2，鏈路連續。

---

## 6. SPARK 自己就是這樣做的

這不是我們的獨創方案，是與原廠參考設計對齊。

**SPARK_AUDIO_DEMO v1.4.0 (2025-12-15) CHANGELOG：**

> - Added a **Mono Fallback mode**.
> - **Increased the retransmission ratio of lower fallback modes** to improve performance.

第二句就是 §3 講的機制，原廠用的是同一個詞。

**他們的主聲道階梯是五階，我們是四階：**

| mode | SPARK Demo | 我們現況 |
|---|---|---|
| 0 | 96k 24-bit | ✅ 相同 |
| 1 | 48k 24-bit | ✅ 相同 |
| 2 | 48k 16-bit | ✅ 相同 |
| 3 | 48k ADPCM Stereo | ✅ 相同 |
| **4** | **48k ADPCM Mono** | ❌ **缺這一階** |

### 我們與原廠參考設計的完整比對結果

逐項比對 `SPARK_AUDIO_DEMO_eng-v1.0.0-ext_codec_support-r1` 與現行 puretone 韌體：

| 項目 | 結果 |
|---|---|
| Timeslot 排程（250 µs × 9） | ✅ 完全相同 |
| DG / HS 時槽分配 | ✅ 完全相同 |
| RF 頻道與跳頻序列 | ✅ 完全相同 |
| **各連線 CCA 設定** | ✅ **完全相同**（逐值一致） |
| FEC ratio、調變方式 | ✅ 完全相同 |
| TX 功率表（monopole） | ✅ 完全相同 |
| ISI mitigation | ⚠️ SPARK = 1，我們 = 0 |
| **Fallback 階梯** | ❌ **SPARK 五階，我們四階** |

**整個無線設定只有兩處不同，其中 ISI 已實測無效** —— 剩下的就是缺的那一階。

---

## 7. 代價

| 項目 | 影響 |
|---|---|
| 立體聲像 | **僅在最底階**失去。該階本來就是「連線快撐不住」時才會進入 |
| 延遲 | 該階 20 ms（現行最底階為 15 ms）—— 越下面越省也越慢，是本架構一貫取捨 |
| 頻寬 | **不變，維持 48 kHz**（不像 24 kHz 方案會掉到 ~7 kHz） |
| RAM | 累積上限 2.3× → 4.2×，緩衝區等比例變大 |

---

## 8. 實作成本

| 項目 | 來源 | 難度 |
|---|---|---|
| 第 5 階模式參數（acc 21/5、latency 20 ms、sample 84） | 抄 SPARK | 低 |
| `SAC_COMPRESSION_PACK_MONO` / `UNPACK_MONO` | **SDK 現成**，back channel 已在使用 | 低 |
| 立體→單聲道降混 / 單→雙還原 | 從 demo 移植 | **中（唯一需要新寫的部分）** |
| 第 4 段 TX 功率（8 個值） | **SPARK 已調校完成，直接抄** | 低 |
| 各設定陣列由 4 項擴為 5 項 | 機械性修改 | 低 |
| 新的重取樣器 / discard 保溫 | **不需要** | — |

原先評估「新增一階」最大的成本是 RF 功率需要重新調校，但原廠參考設計已提供該組數值，
此項成本已消除。

---

## 9. 建議

1. **移植 mono 第五階**，階梯與原廠參考設計對齊
2. 24 kHz 方案保留於 `feat-fallback-24k` 分支，日後若需更省可與 mono 疊加
3. ISI 設為 1（與原廠一致），雖實測無獨立效果，但可消除一項與參考設計的差異

### 待驗證事項（誠實揭露）

§3 的「每包發送次數」是由排程與累積比例**推算**而得，尚未以實測儀器確認。
驗證方式：斷音當下觀察 Coordinator 的 `tx_drop` 計數 —— 若跳動，即代表封包是
「送不出去」而非「送了收不到」，本文假設成立。

若移植 mono 後斷音仍未改善，則可排除整條 RF 路徑，下一個懷疑對象為音訊
pipeline（CDC 時脈漂移補償 / 緩衝管理），而非空中介面。
